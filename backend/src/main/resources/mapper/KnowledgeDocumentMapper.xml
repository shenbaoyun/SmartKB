<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartkb.mapper.KnowledgeDocumentMapper">
    <select id="selectByStatus" resultType="com.smartkb.entity.model.KnowledgeDocumentDO">
        SELECT id, name, file_type, file_size, file_path, chunk_count, status, created_time
        FROM knowledge_document WHERE status = #{status} ORDER BY created_time DESC
    </select>
</mapper>